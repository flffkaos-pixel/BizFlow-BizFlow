# ============================================================
# 하이브리드 완전자동 운영 아키텍처
# GitHub Actions (주기적) + Oracle Cloud (상시) 병행
# ============================================================

## 전체 구성

```
┌──────────────────────────────────────────────────────────────┐
│                 사용자 (고객)                                 │
│      랜딩페이지 → PayPal 결제 → 서비스 사용                    │
└────────────────────────┬─────────────────────────────────────┘
                         │
        ┌────────────────┴────────────────┐
        ▼                                 ▼
┌────────────────────┐          ┌──────────────────────────────┐
│   GitHub Actions   │          │   Oracle Cloud (항상 무료)     │
│   (무료 2000분/월)  │          │   ARM 4코어 24GB RAM          │
├────────────────────┤          ├──────────────────────────────┤
│ · 랜딩페이지 Pages  │          │ · n8n        (자동화)         │
│ · PayPal 결제 감지  │          │ · Dify       (AI 챗봇)       │
│ · 고객 프로비저닝   │──동기화──▶│ · Cal.com    (예약)          │
│ · 리포트 생성       │          │ · Watcher    (크롤러 cron)    │
│ · 모니터링 1차      │          │ · PostgreSQL / Redis         │
└────────────────────┘          │ · Cloudflare Tunnel (HTTPS)  │
                                └──────────────────────────────┘
```

## 역할 분담

| 기능 | 담당 | 이유 |
|------|------|------|
| 랜딩페이지 호스팅 | GitHub Pages | 무료 정적 호스팅, CDN |
| PayPal 결제 | GitHub Actions 폴링 | 상시 서버 불필요 |
| 고객 정보 기록 | GitHub Actions → DB 동기화 | git 기반 백업 겸용 |
| **카톡 AI 챗봇 (실시간)** | **Oracle Dify** | **24시간 상시 응대** |
| **예약 자동화 (실시간)** | **Oracle n8n + Cal.com** | **즉시 처리** |
| 경쟁사 크롤링 | Oracle Watcher (cron) + GH Actions 이중화 | 장애 대비 |
| 월간 리포트 | GH Actions + Oracle 양쪽 | 어떤 쪽이든 생성 |

## 무료 크레딧 계산 (월)

| 항목 | 비용 |
|------|------|
| Oracle Cloud ARM 4코어 24GB | **0원** (항상 무료) |
| GitHub Actions 공개 저장소 | **0원** (2,000분/월) |
| GitHub Pages | **0원** |
| Cloudflare Tunnel + 도메인 | **0원** (도메인 구매비만, ~1만원/년) |
| PayPal 수수료 | 결제액의 2.9% + $0.30 (유일한 비용) |

## 이제 실시간으로 가능해지는 것

1. **카톡 24시간 응대** → Dify 챗봇이 상시 실행
2. **예약 즉시 확정** → n8n이 Cal.com 캘린더와 즉시 연동
3. **경쟁사 실시간 감지** → Watcher cron이 매일 06:00 실행
4. **결제 → 즉시 계정 활성화** → GitHub Actions가 2시간 간격으로 PayPal 확인

## 장애 대비 (이중화)

- Oracle 서버 다운 → GitHub Actions의 monitor-daily가 1차 크롤링 담당
- GH Actions 다운 → Oracle Watcher cron이 리포트 담당
- 둘 다 파일/DB를 각자 보유 → 한쪽만 살아도 운영 지속

## 한 가지 주의

- **PayPal 실결제**는 PayPal 비즈니스 계정 필요 (개인 계정으로는 받기 제한)
- **Cloudflare 도메인**은 구매비 약 1만원/년 (무료 하위도메인은 `trycloudflare.com` 임시 터널로 대체 가능)
- Oracle ARM 할당량은 신규 계정에서 여유가 없을 수 있어 **여러 리전 시도** 필요
